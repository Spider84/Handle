/**
 * @file gpio.h
 * @brief Конфигурация пинов GPIO для N32G430
 * @details Централизованные константы для всех пинов проекта
 */

#ifndef __GPIO_H__
#define __GPIO_H__

#ifdef __cplusplus
extern "C" {
#endif

#include "n32g430.h"
#include "project_config.h"

/* ============================================================================
 * Инициализация GPIO
 * ============================================================================ */
/**
 * @brief Инициализация всех пинов GPIO
 * @details Настраивает начальное состояние всех пинов проекта:
 *          - PA7, PA8, PA10: входы без подтяжек (кнопки)
 *          - PB0: аналоговый вход для детекции чипа (ADC_IN9)
 *          - PB6: выход для BEEPER
 *          - PB1: альтернативная функция для WS2812E (TIM3_CH4)
 */
void gpio_init(void);

/* ============================================================================
 * Управление CHIP_DETECT/CHIP_SELECT (PB0)
 * ============================================================================ */
/**
 * @brief Переключение PB0 в режим CS активен (OUTPUT LOW)
 */
void chip_select_init(void);

/**
 * @brief Переключение PB0 в режим CS неактивен (INPUT для детекции)
 */
void chip_select_deinit(void);

/**
 * @brief Проверка наличия чипа через ADC
 * @return true если чип есть (напряжение > 1.5В), false если нет
 */
bool chip_is_present(void);

#ifdef __cplusplus
}
#endif

#endif /* __GPIO_H__ */
